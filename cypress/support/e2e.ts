import "./commands";

beforeEach(() => {
  // Clear any previous test data from localStorage
  localStorage.clear();
  sessionStorage.clear();
});

afterEach(() => {
  // Optional: Clean up after each test
});
